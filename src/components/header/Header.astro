---
import c from 'classnames'
import s from './Header.module.sass'
import Link from '@components/Link.astro'
// import Link from './Link.astro'

interface Props {
  full?: boolean
}

const { full } = Astro.props
---

<!-- <header class={`Header${full === true ? ' Header--full' : ''}`}> -->
<header class={s.header}>
  <div class={s.container}>
    <nav>
      <a class={s.logo} href="/">Md Mashqur Ul Alam</a>
      <Link class={s.link} href="/about">About</Link>
      <Link class={s.link} href="/blog">Blog</Link>
    </nav>
    <a class={s.avatar} href="/"></a>
  </div>
</header>
